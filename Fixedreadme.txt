Basically fixed the issues to work with the latest version of python, changed some things so it'd work with windows instead.

Just make sure to run this: pip install --upgrade protobuf==3.20.3
and everything should be sorted

-eastoncrafter